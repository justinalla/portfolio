{
  "name": "justin-allaglo-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "check": "astro check",
    "publish:prepare": "astro check && astro build && node scripts/prepare-pages.mjs"
  },
  "dependencies": {
    "astro": "^6.0.0",
    "motion": "^12.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.9.0"
  }
}
